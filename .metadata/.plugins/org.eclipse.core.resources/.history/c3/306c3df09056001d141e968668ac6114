package com.cachatto.cemm.service;

import java.util.HashMap;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;

import com.cachatto.cemm.model.Device;
import com.cachatto.cemm.model.DeviceDetail;
import com.cachatto.cemm.repository.DeviceRepository;


@Service
public class DeviceTypeService {

	@Autowired
	DeviceRepository deviceRepository;
	
	
	public HashMap<String,Long> deviceCount(){
		
		HashMap<String,Long> count = new HashMap<>();
		
		Long androidCount, iosCount;
		androidCount = deviceRepository.findByDeviceType("Android");
		iosCount = deviceRepository.findByDeviceType("Ios");
		
		
		
		count.put("android", androidCount);
		count.put("ios", iosCount);
		
		
		return count;
	}
	
//	 public Page<Device> getDeviceList(Integer pageNo, Integer pageSize, String deviceName){
//
//	        Page<Device> allDevices;
//	        Pageable page = PageRequest.of(pageNo - 1, pageSize);
//	        Long id = DeviceTypeService.getTenant();
//	        if (deviceName == null){
//	        
//	            allDevices =  deviceRepository.findByTenantId(id.intValue(),page);
//	            System.out.println(allDevices);
//	        } 
//	        else {
//	             Integer id_int = id.intValue();
//	            deviceName = deviceName.toLowerCase();
//	           
//	           
//	            allDevices =  deviceRepository.searchByTenantIdAndDeviceNameContaining(id_int,deviceName);
//	        }
//	        return allDevices;
//	    }
//	    private static Long getTenant() {
//		// TODO Auto-generated method stub
//		return null;
//	}
//
//		public DeviceDetail getDeviceDetails(Long id){
//
//	        DeviceDetail allDetails = DeviceRepository.findById(id).get();
//	        return allDetails;
//	    }
}
